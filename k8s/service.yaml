apiVersion: v1
kind: Service
metadata:
  name: task-management-service
spec:
  type: NodePort
  selector:
    app: task-management
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
